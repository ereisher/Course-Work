version: 1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands: []
      <form method="post" action="<?php echo $_SERVER['PHP_SELF'];?>">
  Enter your First Name: <input type="text" name="fname">
  Enter your Password  <input type="text" name="password">
  <input type="submit">
</form>

<?php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    // collect value of input field
    $name = $_POST['fname'];
    $age = $_POST['password'];
    echo "<br>";
    if (empty($name)) {
        echo "Name is empty";
    } else {
        echo "Hello $name! ";
    }
    if (empty($password)) {
        echo "Password is empty";
    } else if (password = "tapplab"){
	echo "Access Granted $name ";
    } else {
        echo "You are $age years old";
    }
}
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
?>
<!DOCTYPE html>
<html>
<body>





